Real trip photography goes here — see `../IMAGE_REQUIREMENTS.md` for the exact filenames
and prompts. Until real files are added, `app.js` falls back to an inline SVG placeholder
for every image, so the site runs cleanly without this folder containing anything.
